version: "3"
services:
  standalone-builder:
    container_name: standalone-builder
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./sources:/sources
      - ./cross-compilers:/cross-compilers